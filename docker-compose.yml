version: '3.6'

services:
  app:
    build: .
    image: nexus-shield
    container_name: nexus-shield-app
    ports: 
      - "8080:8080"
    environment:
      - DB_URL=${DB_URL}
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
    restart: on-failure:3